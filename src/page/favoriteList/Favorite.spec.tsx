import React from "react";
import { render } from "@testing-library/react";

describe("Favorite functions", () => {
  it("Should be able to add to favorite", () => {
    const pokemon = "pica";
    const { debug } = render(<div>asdf</div>);
  });
});
